mixin th(content)
    th(class="mdl-data-table__cell--non-numeric") #{content}

mixin td(content)
    td(class="id mdl-data-table__cell--non-numeric") #{content}
        block

mixin table()
    table(class="mdl-data-table mdl-js-data-table")
        block